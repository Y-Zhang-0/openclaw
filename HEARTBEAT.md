# HEARTBEAT.md

## 触发机制

在飞书群聊的连续 session 环境里，"session 结束"不会发生。改为以下触发：

### 1. 定时触发（每 30 分钟主动自检）
在活跃对话期间，每 30 分钟自动运行一次自验证循环（不打断对话，静默执行）。

### 2. 行动前触发（重大操作前）
在执行以下操作之前，自动先跑自验证：
- Git commit 之前 → 检查是否有未沉淀的 Pattern / 未完成的承诺
- 复杂任务开始前 → 检查是否需要建 Working Buffer
- Cron 创建之前 → 确认 delivery 模式正确

### 3. 被动触发（用户问"自检"时）
用户说"自检一下"→ 立即执行完整自验证并汇报

---

## 自验证循环（每次触发时运行）

### 1. Pattern 提取
- 这 30 分钟有没有新错误/教训？
- 有没有重复场景值得沉淀？
- 用到的 pattern count 有没有更新？

### 2. 承诺检查
- 有没有 skill/cron 承诺了但没落地？（doc-only-commitment）
- 这轮对话里答应了什么还没做？

### 3. 记忆检查
- 重要配置变更有没有写文件？
- 有没有"记脑子里"应该写进去的东西？

---

## 定时任务（独立于自验证）

### 每小时 — Watchdog
检查 gateway 状态，发现挂了用 systemctl restart，记录到 watchdog.log。

### 每 6 小时 — 长期记忆维护
读取 memory/daily/ 目录，提炼到 MEMORY.md，清理过时信息。静默执行。

---

## 子代理调用规范

收到任务时先问：这个适合委派吗？

| 任务类型 | 委派对象 |
|---------|---------|
| 记忆整理、Pattern 提取 | `iris-memory` |
| 编程、Git、命令执行 | `iris-coder` |
| 资讯抓取、RSS、研究 | `iris-news` |
| 自检验证、核对检查 | `iris-check` |

委派时给完整上下文和明确目标，不只说"做这个"。
委派后主代理等待结果，不中途干预。

---

## 进度通知

- 子任务完成 → sessions_spawn announce 自动推送
- 所有子任务完成 → 主 agent 汇总报告
- 遇到阻塞 → 立即推送，不等待

---

## 低频维护

- 记忆文件整理（静默）
- facts.md 更新（静默）
- patterns/candidates/ 低置信度验证（静默）